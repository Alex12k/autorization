# 📖 Руководство администратора

## Управление пользователями в админ панели

### 🔐 Доступ к админ панели

1. Откройте http://localhost:3000/ (или http://localhost:8000/)
2. Нажмите кнопку "Войти в систему"
3. Войдите с учетными данными администратора:
   - **Логин**: `admin`
   - **Пароль**: `admin123`
4. Перейдите в админ панель: http://localhost:3000/admin (или http://localhost:8000/admin)

---

## ✏️ Редактирование пользователей

### Как редактировать пользователя:

1. В таблице пользователей найдите нужного пользователя
2. Нажмите кнопку **редактирования** (иконка карандаша)
3. Откроется модальное окно с формой редактирования
4. Измените необходимые данные:
   - **Имя пользователя** (username)
   - **Email адрес**
   - **Роль** (user/admin)
5. Нажмите **"Сохранить"**

### Что можно изменить:

- ✅ Имя пользователя
- ✅ Email адрес
- ✅ Роль (повысить до админа или понизить до пользователя)

### Ограничения:

- ❌ Нельзя использовать существующий username
- ❌ Нельзя использовать существующий email
- ❌ Email должен быть валидным
- ❌ Роль может быть только `user` или `admin`
- ❌ **Админ не может понизить свою собственную роль** (защита от потери доступа)
- ❌ **Последний админ не может понизить свою роль** (в системе должен быть хотя бы один админ)

### Безопасность:

- 🔒 CSRF защита при каждом изменении
- 🔒 Проверка прав администратора
- 🔒 Валидация всех входных данных
- 🔒 Защита от SQL инъекций
- 🔒 **Защита от потери административного доступа**:
  - Админ не может понизить свою роль
  - Предупреждение в UI при попытке изменения своей роли
  - JavaScript alert с подтверждением
  - Серверная проверка блокирует операцию

---

## 🗑️ Удаление пользователей

### Как удалить пользователя:

1. В таблице пользователей найдите нужного пользователя
2. Нажмите кнопку **удаления** (иконка корзины)
3. Подтвердите удаление в диалоговом окне
4. Пользователь будет удален из базы данных

### Ограничения:

- ❌ Нельзя удалить самого себя (кнопка не отображается)
- ✅ Можно удалить любого другого пользователя, включая других админов

### Безопасность:

- 🔒 CSRF защита
- 🔒 JavaScript подтверждение
- 🔒 Проверка прав администратора
- 🔒 Защита от случайного самоудаления

---

## 📊 Статистика

На странице админ панели отображается:

- **Всего пользователей** - общее количество
- **Администраторов** - количество пользователей с ролью admin
- **Обычных пользователей** - количество пользователей с ролью user
- **За неделю** - количество зарегистрированных за последние 7 дней

---

## 🎨 Уведомления

После каждого действия отображаются уведомления:

### Успешные действия (зеленые):
- ✅ "Пользователь успешно удален"
- ✅ "Пользователь успешно обновлен"

### Ошибки (красные):
- ❌ "Недостаточно прав"
- ❌ "Нельзя удалить свою учетную запись"
- ❌ "Пользователь не найден"
- ❌ "Пользователь с таким именем или email уже существует"
- ❌ "Ошибка безопасности. Попробуйте еще раз."

---

## 🛠️ Технические детали

### Модальное окно редактирования:

- Открывается по клику на кнопку редактирования
- Автоматически заполняется данными пользователя
- Закрывается по:
  - Клику на кнопку "Отмена"
  - Клику вне окна
  - Нажатию клавиши Escape
  - После успешного сохранения

### API функции:

**В файле `components/auth/functions.php`:**

```php
// Редактирование пользователя
updateUserByAdmin(int $user_id, string $username, string $email, string $role): array

// Удаление пользователя  
deleteUser(int $user_id): array

// Получение всех пользователей
getAllUsers(): array
```

### Структура ответа:

```php
[
    'success' => true/false,
    'message' => 'Сообщение об успехе',
    'error' => 'Сообщение об ошибке'
]
```

---

## 🔍 Поиск и фильтрация

В текущей версии доступно:
- Просмотр всех пользователей
- Сортировка по дате регистрации (новые сверху)

**Планируется добавить:**
- Поиск по имени/email
- Фильтрация по роли
- Пагинация

---

## 🛡️ Защита от потери административного доступа

### Что защищено:

**1. Самоизменение роли администратора:**
- Админ **НЕ может** понизить свою собственную роль до `user`
- Это защищает от случайной потери административного доступа
- При попытке изменения появляется:
  - ⚠️ Желтый блок с предупреждением в модальном окне
  - ⚠️ JavaScript alert при смене роли
  - ❌ Ошибка на сервере, если попытка прошла

**2. Защита последнего администратора:**
- Если в системе только один админ, он не может понизить свою роль
- Это гарантирует, что всегда есть доступ к админ панели
- Сообщение: "Нельзя понизить роль последнего администратора"

**3. Когда можно изменить роль админа:**
- ✅ Один админ **может** понизить роль **другого** админа
- ✅ Условие: В системе есть минимум 2 администратора
- ✅ Это позволяет управлять правами других пользователей

### Сценарии использования:

**Сценарий 1: Хочу перестать быть админом**
1. Попросите другого администратора изменить вашу роль
2. Убедитесь, что в системе останется хотя бы один админ
3. Другой админ может понизить вашу роль через редактирование

**Сценарий 2: Нужно добавить нового админа**
1. Найдите пользователя в списке
2. Нажмите "Редактировать"
3. Измените роль на "Admin"
4. Сохраните изменения

**Сценарий 3: Случайно понизили всех админов**
Если это произошло, восстановите админа через базу данных:
```bash
sqlite3 components/auth/database/database.sqlite "UPDATE users SET role = 'admin' WHERE username = 'admin';"
```

---

## 💡 Советы

1. **Регулярно проверяйте список пользователей** на наличие подозрительных аккаунтов
2. **Не удаляйте последнего администратора** - вы не сможете восстановить доступ
3. **Используйте сложные пароли** для администраторских аккаунтов
4. **Проверяйте email адреса** при изменении данных пользователей
5. **Делайте резервные копии базы данных** перед массовыми изменениями
6. **Имейте минимум 2 администратора** для взаимного управления правами
7. **Не пытайтесь понизить свою роль** - система заблокирует это действие

---

## 🆘 Частые проблемы

### Проблема: "Ошибка безопасности"
**Решение**: Обновите страницу и попробуйте снова. CSRF токен устарел.

### Проблема: "Пользователь с таким именем уже существует"
**Решение**: Выберите другое имя пользователя.

### Проблема: Модальное окно не открывается
**Решение**: Проверьте консоль браузера (F12) на наличие JavaScript ошибок.

### Проблема: Изменения не сохраняются
**Решение**: Убедитесь, что вы вошли как администратор и сессия не истекла.

---

## 📞 Контакты

Для вопросов и предложений по улучшению функционала обратитесь к разработчику проекта.
